# Redrob Hackathon — Bug Hunters
# Minimal dependencies: pure Python + lightweight packages only
# No GPU packages, no heavy ML frameworks at inference time

tqdm>=4.65.0
pyyaml>=6.0
streamlit>=1.20.0
pandas>=2.0.0
